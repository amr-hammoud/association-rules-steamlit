import random

def getData(items_csv, num_transactions, max_num_items):
    """
    Generate a dataset of transactions with random items
    """
    
    # transactions = [set(random.sample(items, random.randint(1, max_num_items))) for _ in range(num_transactions)]
    # return transactions


def readFile(uploaded_file):
    """
    Read the uploaded CSV file and return the transactions
    """
    pass